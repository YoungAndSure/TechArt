# 早上一起来，昨天聊过的内容，阅读的进度，一问三不知，为什么？

## session重置时机
- 用户显式重置。发送/new，重置会话
- 过期/定时重置。可配置，超过空闲时间无会话或每天定时重置，默认定时重置。重置是惰性重置，新消息来了判定是否过期，也就是是否要创建新的session。  

## session重置含义
session里保存了全量的用户消息和模型回复，包括模型回复的工具调用及结果。重置意味着相关历史被归档，新的session不能访问。  

## 如何跨session传递信息？  
保存到memory  

# memory存储逻辑  
